# Manuscript

Place the manuscript PDF here if you want the figure-code dump to include it.

The local PDF symlink was intentionally removed because it pointed to a machine-specific path and would break after upload.
