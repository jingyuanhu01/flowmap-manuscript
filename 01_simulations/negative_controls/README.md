# Negative Controls

Simulated negative-control benchmarks that were previously mixed into the real-data benchmark folder.

The notebooks use local `data`, `figures`, and `scripts` paths. `data` points to the shared simulation data folder, and `scripts` points to `flowmap_legacy`.
